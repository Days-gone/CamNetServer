# Requires
1.Wrap the code of the GLCM producer && CAM producer to a Torch Model respectively. The models should only take in a picture and return a picture(PS:a ndarray not a tensor).  
2.Write a test.py to show a test usage code for the two models.  

# Env Setup
You can setup your env with your own methods.  
while if you have trouble with python package version.Here give u a pdm-way to reach it.
first install the pdm in your own python or local machine, then run the command `pdm install` to install the requirements the programme needed.

